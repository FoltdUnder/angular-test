import {HideNotInRangePipe} from './hide-not-in-range.pipe';

describe('HideNotInRangePipe', () => {
  it('create an instance', () => {
    const pipe = new HideNotInRangePipe();
    expect(pipe).toBeTruthy();
  });
});
