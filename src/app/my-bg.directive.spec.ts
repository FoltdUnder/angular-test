import { MyBgDirective } from './my-bg.directive';

describe('MyBgDirective', () => {
  it('should create an instance', () => {
    const directive = new MyBgDirective();
    expect(directive).toBeTruthy();
  });
});
